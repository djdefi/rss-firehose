# frozen_string_literal: true

source 'https://rubygems.org'

# render.rb uses only the Ruby standard library and needs no gems.
# Sinatra is only for the optional local preview server (server.rb).
gem 'sinatra', '~> 2.1'

group :development do
  gem 'minitest'
end
