source 'https://rubygems.org'

# Rendering (render.rb) uses only the Ruby standard library and needs no gems.
# Sinatra is only for the optional local preview server (server.rb).
group :standalone do
  gem 'sinatra', '~> 2.1'
end
